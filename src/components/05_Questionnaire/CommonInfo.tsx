import React from 'react';
import Wrapper from "@/components/common/wrapper";
import style from "@/styles/components/commonInfo.module.scss"

const CommonInfo = () => {
    return (
        <Wrapper className={style.wrapper}>

        </Wrapper>
    );
};

export default CommonInfo;